import React from "react";

const Logo = () => (
  <div className="header-logo">
    <span className="header-logo__img" />
    <span className="header-logo__text">CheckUp</span>
  </div>
);

export default Logo;
